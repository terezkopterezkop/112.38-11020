name: Proxy Slot Worker (VPS 2)

on:
  workflow_dispatch:
    inputs:
      runner_id:
        description: "Proxy slot number"
        required: true
        type: string

permissions:
  contents: read
  actions: write

jobs:
  proxy-slot:
    name: Slot ${{ inputs.runner_id }}
    runs-on: ubuntu-22.04

    concurrency:
      group: proxy-slot-vps2-${{ inputs.runner_id }}
      cancel-in-progress: true

    env:
      RUNNER_ID: ${{ inputs.runner_id }}
      VPS_IP: ${{ secrets.VPS_IP }}

    steps:
      - name: Install packages
        run: |
          sudo apt update
          sudo apt install -y squid autossh curl

      - name: Configure Squid
        run: |
          sudo tee /etc/squid/squid.conf >/dev/null <<'EOF'
          http_port 3128
          acl all src 0.0.0.0/0
          http_access allow all
          visible_hostname github-runner-proxy
          EOF

          sudo systemctl enable squid
          sudo systemctl restart squid
          sleep 3
          ss -lntp | grep ':3128' || true

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/tunnel_key
          chmod 600 ~/.ssh/tunnel_key

          cat > ~/.ssh/config <<EOF
          Host vps2
            HostName ${{ secrets.VPS_IP }}
            User root
            IdentityFile ~/.ssh/tunnel_key
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
            LogLevel ERROR
            ConnectTimeout 10
            ServerAliveInterval 30
            ServerAliveCountMax 3
          EOF

          ssh vps2 "echo SSH_OK"

      - name: Small stagger
        run: |
          sleep $(( ($RUNNER_ID - 1) * 2 ))

      - name: Create reverse tunnel
        run: |
          REMOTE_PORT=$((10000 + RUNNER_ID))
          echo "REMOTE_PORT=$REMOTE_PORT" >> "$GITHUB_ENV"
          echo "PROXY=${VPS_IP}:${REMOTE_PORT}" >> "$GITHUB_ENV"

          ssh vps2 "fuser -k ${REMOTE_PORT}/tcp || true"
          sleep 2

          autossh -M 0 -N -f \
            -o ExitOnForwardFailure=yes \
            -o ServerAliveInterval=30 \
            -o ServerAliveCountMax=3 \
            -R 0.0.0.0:${REMOTE_PORT}:localhost:3128 \
            vps2

          sleep 5

          ssh vps2 "ss -lnt | grep ':${REMOTE_PORT} '" || {
            echo "Tunnel failed on port ${REMOTE_PORT}"
            exit 1
          }

      - name: Verify proxy from VPS
        run: |
          ssh vps2 "curl -x http://127.0.0.1:${REMOTE_PORT} -s --max-time 20 https://api.ipify.org" || {
            echo "Proxy verification failed"
            exit 1
          }

      - name: Register proxy on VPS
        run: |
          ssh vps2 "
            mkdir -p /var/www/html
            touch /var/www/html/proxies-meta.txt /var/www/html/proxies.txt

            grep -v '^${RUNNER_ID}|' /var/www/html/proxies-meta.txt > /tmp/proxies-meta.$$ 2>/dev/null || true
            echo '${RUNNER_ID}|${VPS_IP}:${REMOTE_PORT}' >> /tmp/proxies-meta.$$
            sort -t'|' -k1,1n /tmp/proxies-meta.$$ > /var/www/html/proxies-meta.txt
            cut -d'|' -f2 /var/www/html/proxies-meta.txt > /var/www/html/proxies.txt
            rm -f /tmp/proxies-meta.$$
          "

      - name: Show current list
        run: |
          ssh vps2 "echo '--- proxies.txt'; cat /var/www/html/proxies.txt || true"

      - name: Keep alive
        run: |
          sleep 720

      - name: Cleanup proxy entry
        if: always()
        run: |
          ssh vps2 "
            touch /var/www/html/proxies-meta.txt /var/www/html/proxies.txt
            grep -v '^${RUNNER_ID}|' /var/www/html/proxies-meta.txt > /tmp/proxies-meta.$$ 2>/dev/null || true
            sort -t'|' -k1,1n /tmp/proxies-meta.$$ > /var/www/html/proxies-meta.txt
            cut -d'|' -f2 /var/www/html/proxies-meta.txt > /var/www/html/proxies.txt
            rm -f /tmp/proxies-meta.$$
          "

      - name: Cleanup autossh
        if: always()
        run: |
          pkill autossh || true
